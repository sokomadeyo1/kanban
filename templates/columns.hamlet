<h1 .title>Columns
<div .container>
  $if null columns
    <p>No columns.
  $else
    <ul .columns>
      $forall column <- columns
        <li .column>
          <h3 .column-title>
            <a href=@{ColumnR $ columnTitle column}>#{columnTitle column}
