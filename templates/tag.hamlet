<h1 .tag-name>#{tagname}

<!-- Rename form -->
<form method=post action=@{TagR tagname} enctype=#{enctype}>
  ^{widget}
  <button>Update

<form method=post action=@{DeleteTagR tagname}>
  <button>Delete

<h2>Entries
<div .container>
  $if null entries
    <p>No entries.
  $else
    <ul .entries>
      $forall (entry, tags) <- entries
        <li .entry>
          <span .column-title>#{entryColName entry}
          <span .entry-id>#{pretty $ entryID entry}
          :
          <b .entry-title>#{entryTitle entry}
          <br>
          $forall tag <- tags
            <span .tag-name>#{tagName tag}
          <p .entry-contents>#{entryDesc entry}
          <a href=@{EntryR $ getid entry}>Edit entry
          <hr>
